/* ============================================================
   GYMSHARK SYNC — FRONTEND (entry point)
   ============================================================ */

import { initPage } from './components/bindings.js';

initPage();
